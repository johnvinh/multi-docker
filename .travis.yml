services:
    - docker

before_install:
    - docker build -t react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true react-test npm run test

after_success:
    - docker build -t johnvinh/multi-client ./client
    - docker build -t johnvinh/multi-nginx ./nginx
    - docker build -t johnvinh/multi-server ./server
    - docker build -t johnvinh/multi-worker ./worker
    # Log into the Docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to Docker Hub
    - docker push johnvinh/multi-client
    - docker push johnvinh/multi-nginx
    - docker push johnvinh/multi-server
    - docker push johnvinh/multi-worker